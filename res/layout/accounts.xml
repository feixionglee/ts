<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical" android:layout_width="fill_parent"
	android:layout_height="fill_parent">
	
	  <ListView android:id="@id/android:list"
	        android:layout_width="fill_parent"
	        android:layout_height="fill_parent"/>
	        
	  <TextView android:id="@id/android:empty"
	        android:layout_width="fill_parent"
	        android:layout_height="fill_parent"
	        android:textStyle="italic" android:textSize="18px" android:gravity="center" android:text="Click Add to add new account"/>
   
   
   <LinearLayout android:layout_width="fill_parent" android:layout_height="wrap_content" android:layout_marginTop="5.0dip" android:layout_alignParentBottom="true">
   		<TextView android:id="@+id/aaddaccount"
          android:text="@string/addaccount"
          android:drawableTop="@drawable/ic_menu_invite"
          
          style="@style/detail_btn"/>
   		
   		<TextView android:id="@+id/adelaccount"
          android:text="@string/delaccount"
          android:drawableTop="@drawable/ic_menu_delete"

          style="@style/detail_btn"/>
   
   </LinearLayout>

</RelativeLayout>